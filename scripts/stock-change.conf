# Stock ids for scripts/stock-change.5s.sh.
# Use Tencent format, for example: sz002753, sh601929.
# If only six digits are set, the script infers sh for 60/68/90 and sz otherwise.
ids="sz300394,sz300209"
switch_seconds=6
