# Copy this file to `.env` in the same directory and fill in the values you need.
# Keep `.env` private. It is ignored by Git.

# New API channel usage monitor
NEWAPI_URL=''
NEWAPI_USER_ID='1'
NEWAPI_DAILY_QUOTA='100'
NEWAPI_COOKIE=''

NF_VIDEO_COOKIE=''
DEEPSEEK_TOKEN=''
